name: Prepare All-For-One RC

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Preparation moved to a focused parser-fix phase before the release candidate resumes.'
